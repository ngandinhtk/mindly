{
  "name": "mindly-heroku-root",
  "version": "1.0.0",
  "homepage": "https://ngandinhtk.github.io/mindly",
  "description": "Root for Heroku deployment",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d build",
    "build": "react-scripts build"
  },
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
